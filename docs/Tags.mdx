---
sidebar_custom_props:
    description: Additional tags introduced in Megizen
---

# Tags

<MetaEntry>
    <EntryHeader>
    ### Tag: `EntityTag.modeled_entity`
    </EntryHeader>
    <DetailsTable
        Returns={<code>MegModeledEntityTag</code>}
        Description={
            <>
                Returns the modeled entity of the entity, if any.
            </>
        }
    />
</MetaEntry>
