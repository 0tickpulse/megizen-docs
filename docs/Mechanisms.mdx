---
sidebar_custom_props:
    description: Additional mechanisms introduced in Megizen.
---

# Mechanisms

<MetaEntry>
    <EntryHeader>
    ### Mechanism: `PlayerTag.meg_make_visible`
    </EntryHeader>
    <DetailsTable
        Input = { <code>ElementTag(Boolean)</code> }
        Description = {
            <>
                Forces the player to be visible or invisible. This is a workaround for the invisible player bug when using the <a href="/megizen-docs/command/megmodel"> megmodel</a> command.
            </>
        }
    />
    
</MetaEntry>
